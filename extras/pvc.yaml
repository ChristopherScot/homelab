apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: extras-data
  namespace: extras
spec:
  accessModes: [ReadWriteMany]
  storageClassName: synology-nfs
  resources:
    requests:
      storage: 500Gi
