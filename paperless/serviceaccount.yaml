# SA the SecretStore impersonates for Vault kubernetes-auth (bound to the
# `paperless` Vault role). Required in-namespace; the SecretStore can't
# mint a token for an SA that doesn't exist.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets-sa
  namespace: paperless
