apiVersion: v1
kind: Service
metadata:
  name: {{ include "lgtm-umbrella-chart.fullname" . }}
  labels:
    {{- include "lgtm-umbrella-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lgtm-umbrella-chart.selectorLabels" . | nindent 4 }}
