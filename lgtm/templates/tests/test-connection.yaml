apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lgtm-umbrella-chart.fullname" . }}-test-connection"
  labels:
    {{- include "lgtm-umbrella-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lgtm-umbrella-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
