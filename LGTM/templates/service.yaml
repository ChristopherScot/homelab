apiVersion: v1
kind: Service
metadata:
  name: {{ include "loki-umbrella-chart.fullname" . }}
  labels:
    {{- include "loki-umbrella-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "loki-umbrella-chart.selectorLabels" . | nindent 4 }}
