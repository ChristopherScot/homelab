apiVersion: v1
kind: Service
metadata:
  name: test-service
  namespace: default
  labels:
    app: test-service
    team: fake-team
spec:
  ports:
  - port: 80
    targetPort: 80
    nodePort: 32000
  selector:
    app: test-service
    someotherThing: value
